package GUI;

import javax.swing.*;

public class MainFrame extends JFrame {
    public MainFrame(){
        setLayout(null);
        setSize(500, 500);
        setTitle("Project Tore");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
    }
}
